import React from 'react';

const headerStyle = {
    width: '100%',
    height: '100px',
    background: "green" ,
};

const Header = (props) => {
   
    return (
        <div style={headerStyle}>
        </div>
    );
}
    
export default Header;
